{
  "name": "yt-hist-desktop",
  "version": "0.1.0",
  "private": true,
  "description": "Electron shell for local yt-hist backend and frontend",
  "main": "electron/main.cjs",
  "scripts": {
    "dev": "export FRONTEND_PORT=$(node scripts/setup-dev-environment.js frontend) && export BACKEND_PORT=$(node scripts/setup-dev-environment.js backend) && concurrently -k \"pnpm --dir frontend dev\" \"wait-on tcp:$FRONTEND_PORT && electron electron/main.cjs\"",
    "start": "electron electron/main.cjs",
    "build:frontend": "pnpm --dir frontend build"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "electron": "^35.0.3",
    "wait-on": "^8.0.3"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "electron"
    ]
  }
}
